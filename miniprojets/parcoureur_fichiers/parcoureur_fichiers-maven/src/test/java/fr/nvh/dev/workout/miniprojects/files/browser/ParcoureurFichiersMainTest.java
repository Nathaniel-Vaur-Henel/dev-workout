package fr.nvh.dev.workout.miniprojects.files.browser;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

/** Ce test n'est qu'un exemple et doit être adapté au véritable besoin. */
class ParcoureurFichiersMainTest {
  @Test
  void createYourOwnTest() {
    assertEquals(true, true);
  }
}
